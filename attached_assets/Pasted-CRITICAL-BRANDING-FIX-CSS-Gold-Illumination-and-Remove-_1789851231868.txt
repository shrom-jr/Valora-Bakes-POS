CRITICAL BRANDING FIX: CSS Gold Illumination and Remove Navbar White Circle

The bakery logo at `public/logo.png` is the final official transparent PNG and must not be edited. Adjust the application CSS and layout so this transparent asset displays with high contrast and visibility across all screens:

1. Lift the Gold Tones with CSS (App-Wide Rule):
- The logo's dark bronze tones blend into the obsidian background.
- Apply a CSS filter to the logo image across the app:
  `filter: brightness(1.7) contrast(1.1) drop-shadow(0 0 8px rgba(245, 158, 11, 0.35))`
- This brightens the dark bronze "VALORA" lettering and flourishes into clean, visible metallic gold without modifying the source image.

2. Command Bar (Navbar Component):
- Completely REMOVE `bg-white`, white circular discs, white backgrounds, and white borders around the logo.
- Render the transparent logo cleanly inside a sleek dark frame:
  - Container: `h-10 w-10 flex items-center justify-center rounded-xl bg-[#14161B] border border-amber-500/30`
  - Image: `h-8 w-8 object-contain filter brightness-[1.7] contrast-[1.1] drop-shadow-[0_0_6px_rgba(245,158,11,0.4)]`
- Keep the clean text lockup beside it ("Bakery POS" or "Valora Bakes").

3. Sign-In Modal (SignIn.tsx):
- Scale up the logo container so the artwork is clearly legible:
  - Size: `h-28 w-28` (112px) centered above the title.
  - Image: `h-full w-full object-contain filter brightness-[1.75] contrast-[1.1] drop-shadow-[0_0_18px_rgba(245,158,11,0.45)]`
- Add a soft ambient back-light wrapper behind the logo:
  `relative flex items-center justify-center mx-auto mb-4 p-2 rounded-2xl bg-gradient-to-b from-amber-500/10 to-transparent border border-amber-500/20`

4. Store Settings (/dashboard/settings):
- In the "Business Profile" header:
  - Container: `h-12 w-12 flex items-center justify-center rounded-xl bg-[#14161B] border border-amber-500/30`
  - Image: `h-9 w-9 object-contain filter brightness-[1.7] contrast-[1.1]`

5. Thermal Receipts:
- For `@media print`, ensure the logo uses `filter: grayscale(100%) contrast(200%) brightness(0.9)` so the transparent line-work burns cleanly on thermal paper.